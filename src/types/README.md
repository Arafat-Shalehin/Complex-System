# TypeScript Types

Contains shared TypeScript types and interfaces.

Examples:
- User types
- Account types
- Transaction types
- API response types

Goal:
Avoid duplicating type definitions throughout the project.